package com.san.farm.util;

import java.io.File;

/**
 * Stub interface for COS (O'Reilly) FileRenamePolicy.
 * Note: This is a minimal stub to allow compilation.
 * Please install the actual COS library (cos-05Nov2002.jar) for production use.
 */
public interface FileRenamePolicy {
    File rename(File f);
}

