# ---------------------------------------------------------------
# Build-time environment stamp — generated by Maven resource
# filtering. Do NOT edit this file manually; values are injected
# from the active Maven profile at package time.
#
#   mvn package -P dev    ->  build.env=dev,  license.ui.editable=true
#   mvn package -P prod   ->  build.env=prod, license.ui.editable=false
# ---------------------------------------------------------------
build.env=${build.env}
license.ui.editable=${license.ui.editable}
