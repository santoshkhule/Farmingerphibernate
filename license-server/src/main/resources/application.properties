server.port=8085

# Admin UI login key — required to access /admin dashboard
license.admin.key=sevak-admin-2024

# API key for /api/license/validate — leave empty to keep endpoint open (no auth)
# When set, callers must include header: X-Api-Key: <value>
license.api.key=

license.data.dir=${user.home}/.sevak-license
spring.thymeleaf.cache=false
